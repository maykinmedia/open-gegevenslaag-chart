apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "open-gegevenslaag.fullname" . }}-test-connection"
  labels:
    {{- include "open-gegevenslaag.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "open-gegevenslaag.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
