apiVersion: v1
kind: Service
metadata:
  name: {{ include "open-gegevenslaag.fullname" . }}
  labels:
    {{- include "open-gegevenslaag.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "open-gegevenslaag.selectorLabels" . | nindent 4 }}
